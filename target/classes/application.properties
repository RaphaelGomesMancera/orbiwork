# Host obrigatório no Render
quarkus.http.host=0.0.0.0

# Swagger
quarkus.smallrye-openapi.path=/openapi
quarkus.swagger-ui.path=/swagger
quarkus.swagger-ui.always-include=true

# Tipo do banco
quarkus.datasource.db-kind=postgresql

# URL / usuário / senha
# Defaults com base no banco do Render (External Database URL)
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://dpg-d4hnmk0gjchc73evc52g-a.ohio-postgres.render.com:5432/orbiwork_db?sslmode=require}
quarkus.datasource.username=${DB_USER:orbiwork_db_user}
quarkus.datasource.password=${DB_PASSWORD:dGUXzWY8PeRxWVyR5XfchH33LAI8KRfm}

# Script para criar tabelas TRILHA, CURSO, USUARIO
quarkus.datasource.jdbc.initialization-script-path=schema.sql
